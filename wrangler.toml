#:schema node_modules/wrangler/config-schema.json
name = "the-mosaic-theory"
pages_build_output_dir = "dist"

[compatibility]
compatibility_date = "2024-11-01"
compatibility_flags = [ "nodejs_compat" ]

[vars]
# Add default public variables if any. Secrets like GEMINI_API_KEY should be set
# securely through the Cloudflare Pages settings dashboard.
VITE_CLOUDFLARE_PAGES = "true"
